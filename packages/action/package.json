{
  "name": "@lockray/action",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/types.d.ts",
  "files": ["dist", "action.yml"],
  "scripts": {
    "build": "tsc -b",
    "bundle": "rm -rf dist && ncc build src/main.ts -o dist --license licenses.txt",
    "clean": "tsc -b --clean && rm -rf dist"
  },
  "dependencies": {
    "@actions/artifact": "^2.2.0",
    "@actions/core": "^1.11.0",
    "@actions/exec": "^1.1.1",
    "@actions/github": "^6.0.0",
    "@lockray/types": "*"
  },
  "devDependencies": {
    "@lockray/scoring": "*",
    "@vercel/ncc": "^0.38.0"
  }
}
